{
  "@": "${cwd}/src",
  "api": "${cwd}/src/api",
  "assets": "${cwd}/src/assets",
  "components": "${cwd}/src/components",
  "plugins": "${cwd}/src/plugins",
  "store": "${cwd}/src/store",
  "utils": "${cwd}/src/utils",
  "views": "${cwd}/src/views"
}
